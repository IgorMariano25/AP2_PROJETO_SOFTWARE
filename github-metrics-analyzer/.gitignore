repos/

__pycache__/
*.pyc
.venv/
venv/

# Tool downloads and temp (CK JAR, redirected TEMP, CK output)
tools/
.tmp/
data/pilot_semgrep.json

# Estado intermediário/retomável das coletas (consolidado depois em data/*.csv)
data/partial/

# Secrets - never commit tokens/credentials
.env
.env.*
!.env.example
*.token
*.secret
secrets.json

# Arquivos grandes (>100 MB) versionados via Git LFS:
#   data/complexity.csv, metrics.db  -> ver .gitattributes

